# عقد حماية بيانات بوابة أستاذ لحوني

هذا الملف جزء من نظام حماية البيانات، ويجب مراجعته قبل أي تعديل مستقبلي.

## قواعد إلزامية

1. تعديلات التصميم يجب أن تكون في ملفات CSS أو مكونات العرض فقط.
2. يمنع تغيير جذر بيانات المعلم والمادة:
   - `portalV2Data/{teacherId}/subjects/{subjectKey}`
3. يمنع حذف أو إعادة تسمية المجموعات التالية دون ترحيل ونسخة احتياطية:
   - `students`
   - `classes`
   - `attendance`
   - `timetable`
   - `diagnostics`
   - `diagnosticResults`
   - `treatmentPlans`
   - بيانات الدرجات داخل وثائق الطلاب
4. يمنع استخدام `localStorage.clear()` داخل البوابة.
5. تسجيل الخروج يمسح الجلسة فقط، ولا يمسح القوائم أو الحضور أو الدرجات أو الاختبارات.
6. عند تغيير اسم مفتاح محلي، يجب قراءة المفتاح القديم وترحيله قبل إزالته.
7. كل إصدار تصميم جديد يُضاف كطبقة مستقلة مستوردة في نهاية `app/layout.tsx`، ليكون الرجوع عنه آمنًا.
8. قبل النشر يجب أن ينجح `next build` وفحص TypeScript، ثم تُفحص بوابات الطالب والمعلم والإدارة على رابط النشر.

## الحماية الموجودة حاليًا

- `app/data-safety-guard.tsx` يحتفظ بنسختين تلقائيتين من البيانات المحلية المهمة.
- عند اختفاء مفتاح بيانات محمي من المتصفح، تتم استعادته من أحدث نسخة متاحة.
- مفاتيح الجلسة والدخول غير مشمولة بالاستعادة حتى يظل تسجيل الخروج آمنًا.
- تصميم V40 موجود في `app/portal-redesign-v40.css` ولا يغيّر أي مسار بيانات.

## مسار آمن لأي تعديل قادم

1. عدم تعديل ملفات البيانات أثناء تغيير التصميم.
2. تنفيذ التغيير في ملف مستقل.
3. فحص الفرق البرمجي والتأكد أن مسارات Firebase ومفاتيح الحفظ لم تتغير.
4. بناء المشروع وفحص الصفحات.
5. نشر نسخة قابلة للرجوع إلى الإصدار السابق فورًا عند ظهور مشكلة.
