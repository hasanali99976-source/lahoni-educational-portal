"use client";

import { jsPDF } from "jspdf";

export type GradebookPdfColumn={id:string;label:string;max:number};
export type GradebookPdfRow={number:number;name:string;values:number[];sectionTotal:number;overallTotal:number;percentage:number};
export type GradebookPdfSection={id:string;label:string;max:number;columns:GradebookPdfColumn[];rows:GradebookPdfRow[]};
export type GradebookPdfClass={className:string;sections:GradebookPdfSection[];accentColor?:string};
export type GradebookPdfDocumentOptions={portalName:string;teacherName:string;subject:string;gradeLabel?:string;planLabel:string;planVersion:number;classes:GradebookPdfClass[];fileName:string};

const WIDTH=1680,HEIGHT=1188,MAX_COLUMNS=6;
const ACCENTS=["#0b675f","#365b94","#71509a","#9a5c39","#3b785d","#8a681e","#8b4560","#4a6689"];
function activeFontFamily(){if(typeof window!=="undefined"&&document?.body){const value=getComputedStyle(document.body).fontFamily;if(value)return value;}return"Tajawal, Arial, sans-serif";}
function chunks<T>(items:T[],size:number){if(!items.length)return[[]] as T[][];return Array.from({length:Math.ceil(items.length/size)},(_,i)=>items.slice(i*size,(i+1)*size));}
function page(){const canvas=document.createElement("canvas");canvas.width=WIDTH;canvas.height=HEIGHT;const ctx=canvas.getContext("2d");if(!ctx)throw new Error("grades_pdf_canvas_unavailable");ctx.fillStyle="#fff";ctx.fillRect(0,0,WIDTH,HEIGHT);ctx.textBaseline="middle";ctx.direction="rtl";return{canvas,ctx};}
function font(ctx:CanvasRenderingContext2D,size:number,weight=700){ctx.font=`${weight} ${size}px ${activeFontFamily()}`;}
function fit(ctx:CanvasRenderingContext2D,value:string,maxWidth:number,preferred:number,min:number,weight=700){let size=preferred;while(size>min){font(ctx,size,weight);if(ctx.measureText(value).width<=maxWidth)break;size-=.5;}return size;}
function text(ctx:CanvasRenderingContext2D,value:unknown,x:number,y:number,o:{size?:number;min?:number;weight?:number;color?:string;align?:CanvasTextAlign;maxWidth?:number}={}){const raw=String(value??"");const weight=o.weight??700;const size=o.maxWidth?fit(ctx,raw,o.maxWidth,o.size??18,o.min??9,weight):(o.size??18);font(ctx,size,weight);ctx.fillStyle=o.color??"#173d45";ctx.textAlign=o.align??"right";ctx.fillText(raw,x,y);}
function rounded(ctx:CanvasRenderingContext2D,x:number,y:number,w:number,h:number,r:number,fill:string,stroke?:string){const rr=Math.min(r,w/2,h/2);ctx.beginPath();ctx.moveTo(x+rr,y);ctx.arcTo(x+w,y,x+w,y+h,rr);ctx.arcTo(x+w,y+h,x,y+h,rr);ctx.arcTo(x,y+h,x,y,rr);ctx.arcTo(x,y,x+w,y,rr);ctx.closePath();ctx.fillStyle=fill;ctx.fill();if(stroke){ctx.strokeStyle=stroke;ctx.lineWidth=1.4;ctx.stroke();}}
function line(ctx:CanvasRenderingContext2D,x1:number,y1:number,x2:number,y2:number,color="#d7e2df",width=1.2){ctx.beginPath();ctx.moveTo(x1,y1);ctx.lineTo(x2,y2);ctx.strokeStyle=color;ctx.lineWidth=width;ctx.stroke();}
function metrics(rows:GradebookPdfRow[],section:GradebookPdfSection){const count=rows.length||1;const averageSection=Math.round((rows.reduce((sum,row)=>sum+row.sectionTotal,0)/count)*10)/10;const averageOverall=Math.round((rows.reduce((sum,row)=>sum+row.percentage,0)/count)*10)/10;const excellent=rows.filter(row=>row.percentage>=90).length;const support=rows.filter(row=>row.percentage>0&&row.percentage<60).length;const sectionRate=section.max?Math.round((averageSection/section.max)*100):0;return{averageSection,averageOverall,excellent,support,sectionRate};}
async function loadPortalLogo(){return new Promise<HTMLImageElement|null>(resolve=>{const image=new Image();image.onload=()=>resolve(image);image.onerror=()=>resolve(null);image.src="/icons/lahooni-identity-320.jpg";});}
function header(ctx:CanvasRenderingContext2D,options:GradebookPdfDocumentOptions,report:GradebookPdfClass,section:GradebookPdfSection,accent:string,classIndex:number,colIndex:number,colCount:number,logo:HTMLImageElement|null){
  rounded(ctx,34,28,WIDTH-68,136,26,"#fff","#dce8e4");rounded(ctx,WIDTH-376,28,342,136,26,accent);
  text(ctx,"منصة تعليمية ذكية",WIDTH-348,63,{size:16,weight:950,color:"#dcefeb",maxWidth:285});text(ctx,"التحصيل العلمي",WIDTH-348,106,{size:30,min:22,weight:950,color:"#fff",maxWidth:285});
  rounded(ctx,48,40,112,112,24,"#fff","#d6e3df");if(logo)ctx.drawImage(logo,56,48,96,96);
  text(ctx,options.portalName,178,66,{size:20,weight:950,color:"#5f777c",align:"left",maxWidth:520});text(ctx,"تقرير الرصد والتحصيل الأكاديمي",178,111,{size:32,min:23,weight:950,color:"#173d45",align:"left",maxWidth:720});
  rounded(ctx,178,136,290,24,12,"#f3f7f6");text(ctx,`الفصل ${classIndex+1} من ${options.classes.length}${colCount>1?` • الجزء ${colIndex+1}/${colCount}`:""}`,323,148,{size:12.5,weight:900,color:accent,align:"center",maxWidth:270});
  const meta=[["المعلم",options.teacherName],["المادة",options.subject],["المرحلة",options.gradeLabel||"—"],["الفصل",report.className],["الوحدة / الفترة",section.label]];const gap=10,margin=34,top=180,boxW=(WIDTH-margin*2-gap*4)/5;
  meta.forEach(([label,value],i)=>{const x=WIDTH-margin-boxW-i*(boxW+gap);rounded(ctx,x,top,boxW,64,13,"#f8fbfa","#dce7e4");text(ctx,label,x+boxW-14,top+19,{size:11.5,weight:900,color:"#859598",maxWidth:boxW-28});text(ctx,value,x+boxW-14,top+44,{size:16.5,min:11,weight:950,color:"#21464c",maxWidth:boxW-28});});
  const m=metrics(section.rows,section);const summary=[["متوسط الوحدة",`${m.averageSection} / ${section.max}`],["نسبة الوحدة",`${m.sectionRate}%`],["متوسط التحصيل",`${m.averageOverall}%`],["متميزون",m.excellent],["يحتاجون دعمًا",m.support],["الخطة",`نسخة ${options.planVersion}`]] as const;const sw=(WIDTH-68-gap*5)/6;
  summary.forEach(([label,value],i)=>{const x=WIDTH-34-sw-i*(sw+gap);const highlight=i===2||i===3;rounded(ctx,x,258,sw,62,13,highlight?"#eaf6f1":"#f7faf9","#dfe8e6");text(ctx,value,x+sw/2,278,{size:18.5,min:11,weight:950,color:highlight?"#216c4c":"#244b51",align:"center",maxWidth:sw-20});text(ctx,label,x+sw/2,302,{size:11,weight:900,color:"#758b8e",align:"center"});});
}
function table(ctx:CanvasRenderingContext2D,section:GradebookPdfSection,columns:Array<{column:GradebookPdfColumn;originalIndex:number}>,accent:string){
  const x=34,top=338,w=WIDTH-68,bottom=HEIGHT-100,headerH=58;const rows=section.rows;const minRows=Math.max(24,rows.length);const rowH=Math.max(14,Math.floor((bottom-top-headerH)/minRows));const compact=rows.length>34;
  const numW=60,nameW=330,totalW=130,overallW=125,percentW=105;const fixed=numW+nameW+totalW+overallW+percentW;const gradeW=(w-fixed)/Math.max(columns.length,1);rounded(ctx,x,top,w,bottom-top,14,"#fff","#cbdad6");ctx.save();ctx.beginPath();ctx.rect(x,top,w,bottom-top);ctx.clip();ctx.fillStyle=accent;ctx.fillRect(x,top,w,headerH);
  let cursor=x+w;const head=(width:number,label:string,sub="")=>{const c=cursor-width/2;text(ctx,label,c,top+21,{size:13.5,min:9.5,weight:950,color:"#fff",align:"center",maxWidth:width-10});if(sub)text(ctx,sub,c,top+42,{size:10,min:7.5,weight:850,color:"#dcefeb",align:"center",maxWidth:width-10});cursor-=width;line(ctx,cursor,top,cursor,bottom,"rgba(255,255,255,.25)",1);};
  head(numW,"م");head(nameW,"اسم الطالب");columns.forEach(({column})=>head(gradeW,column.label,`من ${column.max}`));head(totalW,"مجموع الوحدة",`من ${section.max}`);head(overallW,"المجموع الحالي","من 100");head(percentW,"التحصيل","%");
  rows.forEach((row,index)=>{const y=top+headerH+index*rowH;ctx.fillStyle=index%2?"#f8fbfa":"#fff";ctx.fillRect(x,y,w,rowH);line(ctx,x,y+rowH,x+w,y+rowH,"#e4ecea");let r=x+w;const cell=(width:number,value:unknown,o:{name?:boolean;strong?:boolean;color?:string}={})=>{if(o.name)text(ctx,value,r-13,y+rowH/2,{size:compact?11.5:14,min:9.5,weight:950,color:o.color,maxWidth:width-26});else text(ctx,value,r-width/2,y+rowH/2,{size:compact?10.5:12.5,min:8,weight:o.strong?950:850,color:o.color,align:"center",maxWidth:width-9});r-=width;line(ctx,r,y,r,y+rowH);};
    cell(numW,row.number,{strong:true});cell(nameW,row.name,{name:true});columns.forEach(({originalIndex})=>cell(gradeW,row.values[originalIndex]??0));cell(totalW,row.sectionTotal,{strong:true,color:accent});cell(overallW,row.overallTotal,{strong:true,color:"#226e4d"});cell(percentW,`${row.percentage}%`,{strong:true});
  });ctx.restore();
}
function footer(ctx:CanvasRenderingContext2D,options:GradebookPdfDocumentOptions,report:GradebookPdfClass,section:GradebookPdfSection,accent:string){const y=HEIGHT-64;line(ctx,34,y-20,WIDTH-34,y-20,"#c8d7d3",1.3);text(ctx,"اعتماد المعلم: __________________________",WIDTH-34,y,{size:12.5,weight:850,color:"#647b80",maxWidth:450});text(ctx,"اعتماد الإدارة: __________________________",WIDTH/2,y,{size:12.5,weight:850,color:"#647b80",align:"center",maxWidth:450});text(ctx,`${report.className} • ${section.label} • ${options.planLabel}`,34,y,{size:12,min:9,weight:950,color:accent,align:"left",maxWidth:540});}
function render(options:GradebookPdfDocumentOptions,report:GradebookPdfClass,section:GradebookPdfSection,classIndex:number,columns:Array<{column:GradebookPdfColumn;originalIndex:number}>,colIndex:number,colCount:number,logo:HTMLImageElement|null){const {canvas,ctx}=page();const accent=report.accentColor||ACCENTS[classIndex%ACCENTS.length]||ACCENTS[0];header(ctx,options,report,section,accent,classIndex,colIndex,colCount,logo);table(ctx,section,columns,accent);footer(ctx,options,report,section,accent);return canvas;}

export async function downloadGradebookPdfDocument(options:GradebookPdfDocumentOptions){
  const classes=options.classes.filter(item=>item.sections.some(section=>section.rows.length));if(!classes.length)throw new Error("gradebook_pdf_no_students");if(document.fonts?.ready)await document.fonts.ready;
  const logo=await loadPortalLogo();
  const pages:Array<{report:GradebookPdfClass;section:GradebookPdfSection;classIndex:number;columns:Array<{column:GradebookPdfColumn;originalIndex:number}>;colIndex:number;colCount:number}>=[];
  classes.forEach((report,classIndex)=>report.sections.filter(section=>section.rows.length).forEach(section=>{const indexed=section.columns.map((column,originalIndex)=>({column,originalIndex}));const groups=chunks(indexed,MAX_COLUMNS);groups.forEach((columns,colIndex)=>pages.push({report,section,classIndex,columns,colIndex,colCount:groups.length}));}));
  const pdf=new jsPDF({orientation:"landscape",unit:"mm",format:"a4",compress:true});pages.forEach((item,index)=>{const canvas=render(options,item.report,item.section,item.classIndex,item.columns,item.colIndex,item.colCount,logo);if(index)pdf.addPage("a4","landscape");pdf.addImage(canvas.toDataURL("image/png"),"PNG",0,0,297,210,undefined,"FAST");});
  pdf.save(options.fileName);return{pageCount:pages.length,classCount:classes.length,studentCount:classes.reduce((sum,item)=>sum+Math.max(0,...item.sections.map(section=>section.rows.length)),0)};
}
