import { cookies } from "next/headers";
import { unstable_cache } from "next/cache";
import { NextResponse } from "next/server";
import { adminDb } from "../../../../lib/server/firebase-admin";
import { requireSession } from "../../../../lib/server/portal-auth";
import { normalizeAssignments } from "../../../../lib/teacher-assignments";
import { SCHOOL_CLASSES_COLLECTION, SCHOOL_STUDENTS_COLLECTION, canonicalClassName, classId, gradeNumber, normalizeClassRecord, normalizeStudentRecord, type SchoolClass, type SchoolStudent } from "../../../../lib/school-roster";
import { TEACHER_CLASS_SCOPES_COLLECTION, assignmentScopeSignature, defaultSelectedClassIds, normalizeClassIds, teacherClassScopeId } from "../../../../lib/teacher-class-scope";
type Grade=1|2|3; type CachedDocument={id:string;data:Record<string,unknown>}; type RosterResponse=Record<string,unknown>;
function cacheSafeData(v:FirebaseFirestore.DocumentData){return JSON.parse(JSON.stringify(v)) as Record<string,unknown>}
const responseCache=new Map<string,{value:RosterResponse;expiresAt:number}>(); const responseInflight=new Map<string,Promise<RosterResponse>>(); const RESPONSE_TTL_MS=60*60*1000;
const readCentralStudents=unstable_cache(async(gradeKey:string):Promise<CachedDocument[]>=>{const grades=gradeKey.split(",").map(Number).filter(g=>g>=1&&g<=3);if(!grades.length)return[];const snap=await adminDb().collection(SCHOOL_STUDENTS_COLLECTION).where("grade","in",grades).get();return snap.docs.map(d=>({id:d.id,data:cacheSafeData(d.data())}))},["teacher-central-roster-v4"],{revalidate:3600,tags:["teacher-central-roster"]});
const readCentralClasses=unstable_cache(async(gradeKey:string):Promise<CachedDocument[]>=>{const grades=gradeKey.split(",").map(Number).filter(g=>g>=1&&g<=3);if(!grades.length)return[];const snap=await adminDb().collection(SCHOOL_CLASSES_COLLECTION).where("grade","in",grades).get();return snap.docs.map(d=>({id:d.id,data:cacheSafeData(d.data())}))},["teacher-central-classes-v4"],{revalidate:3600,tags:["teacher-central-roster"]});
function classGradeFromId(v:string){const g=Number(v.split("-")[0]);return g===1||g===2||g===3?g as Grade:null}
function gradeFromWorkspace(v:string,subjectId:string):Grade|null{const m=String(v||"").trim().lastIndexOf("--");if(m<0)return null;const w=String(v||"").trim();if(w.slice(0,m)!==subjectId)return null;const g=Number(w.slice(m+2)||0);return g===1||g===2||g===3?g as Grade:null}
function normalizeOfficial(doc:CachedDocument):SchoolStudent|null{const s=normalizeStudentRecord(doc.data,doc.id);if(!s||s.active===false)return null;const grade=gradeNumber(s.grade);const section=String(s.section||"").replace(/[^0-9]/g,"");if(!grade||!section)return null;return {...s,id:s.code,code:s.code,grade,section,className:canonicalClassName(grade,section),active:true,officialRoster:true} as SchoolStudent}
async function buildRoster(session:NonNullable<Awaited<ReturnType<typeof requireSession>>>,subjectId:string,requestedGrade:Grade|null):Promise<RosterResponse>{const assignments=normalizeAssignments(session.user!.assignments,session.user!.subjectIds);const relevantAll=assignments.filter(a=>a.subjectId===subjectId);const relevant=requestedGrade?relevantAll.filter(a=>gradeNumber(a.grade)===requestedGrade):relevantAll;const grades=new Set<Grade>(relevantAll.map(a=>gradeNumber(a.grade)).filter((g):g is Grade=>!!g));if(requestedGrade&&!grades.has(requestedGrade))return{ok:true,students:[],classes:[],availableClasses:[],selectedClassIds:[],assignments:[]};if(requestedGrade){grades.clear();grades.add(requestedGrade)}if(!subjectId||!relevant.length||!grades.size)return{ok:true,students:[],classes:[],availableClasses:[],selectedClassIds:[],assignments:relevant};const db=adminDb();const gradeKey=[...grades].sort().join(",");const scopeRef=db.collection(TEACHER_CLASS_SCOPES_COLLECTION).doc(teacherClassScopeId(session.userId,subjectId,requestedGrade));const[scopeSnapshot,studentDocs,classDocs]=await Promise.all([scopeRef.get(),readCentralStudents(gradeKey),readCentralClasses(gradeKey)]);const official=studentDocs.map(normalizeOfficial).filter((s):s is SchoolStudent=>!!s&&grades.has(s.grade as Grade)&&Number(s.section)>0);const availableMap=new Map<string,SchoolClass>();classDocs.forEach(d=>{const c=normalizeClassRecord({id:d.id,...d.data} as Partial<SchoolClass>);if(c&&c.active!==false&&grades.has(c.grade as Grade))availableMap.set(c.id,c)});official.forEach(s=>{const id=classId(s.grade,s.section);if(!availableMap.has(id))availableMap.set(id,{id,grade:s.grade,section:s.section,name:canonicalClassName(s.grade,s.section),active:true})});const allClasses=[...availableMap.values()].filter(c=>/^\d+-\d+$/.test(c.id)).sort((a,b)=>a.grade-b.grade||Number(a.section)-Number(b.section));const sig=assignmentScopeSignature(assignments,subjectId,requestedGrade);const sd=scopeSnapshot.exists?scopeSnapshot.data() as Record<string,unknown>:null;const stored=normalizeClassIds(sd?.selectedClassIds).filter(id=>availableMap.has(id)&&grades.has(classGradeFromId(id) as Grade));const customized=sd?.customized===true&&String(sd?.assignmentSignature||"")===sig;const defaults=defaultSelectedClassIds(relevant,subjectId,allClasses,requestedGrade);const selectedClassIds=[...new Set(customized?stored:defaults)].filter(id=>availableMap.has(id)&&grades.has(classGradeFromId(id) as Grade));const selected=new Set(selectedClassIds);const byCode=new Map<string,SchoolStudent>();official.filter(s=>selected.has(classId(s.grade,s.section))).forEach(s=>{if(s.code)byCode.set(String(s.code).trim(),s)});const students=[...byCode.values()].sort((a,b)=>a.grade-b.grade||Number(a.section)-Number(b.section)||a.name.localeCompare(b.name,"ar"));const classes=allClasses.filter(c=>selected.has(c.id));return{ok:true,students,classes,availableClasses:allClasses,selectedClassIds,scopeCustomized:customized,assignments:relevant,assignedGrades:[...grades],activeGrade:requestedGrade,officialAdminRoster:true,preservedTeacherData:true,deduplicatedStudentCodes:students.length,centralReadCount:studentDocs.length,classReadCount:classDocs.length}}
export async function GET(request:Request){const session=await requireSession("teacher");if(!session||!session.user)return NextResponse.json({ok:false},{status:401});try{const url=new URL(request.url);const subjectId=String(url.searchParams.get("subjectId")||"").trim();const cookieStore=await cookies();const wg=gradeFromWorkspace(cookieStore.get("lahooni_active_subject")?.value||"",subjectId);const gv=Number(url.searchParams.get("grade")||wg||0);const requested:Grade|null=gv===1||gv===2||gv===3?gv as Grade:null;const key=`${session.userId}:${subjectId}:${requested||"all"}`;const cached=responseCache.get(key);if(cached&&cached.expiresAt>Date.now())return NextResponse.json(cached.value,{headers:{"Cache-Control":"private, max-age=300, stale-while-revalidate=3300"}});let pending=responseInflight.get(key);if(!pending){pending=buildRoster(session,subjectId,requested);responseInflight.set(key,pending)}const value=await pending;responseInflight.delete(key);responseCache.set(key,{value,expiresAt:Date.now()+RESPONSE_TTL_MS});return NextResponse.json(value,{headers:{"Cache-Control":"private, max-age=300, stale-while-revalidate=3300"}})}catch(error){console.error("teacher central roster failed",error);return NextResponse.json({ok:false,message:"تعذر تحميل قائمة الطلاب"},{status:500})}}
