"use client";

import { useEffect, useMemo, useState } from "react";
import { doc, setDoc } from "firebase/firestore";
import { db } from "../../../lib/firebase";
import { tenantCollection } from "../../../lib/teacher-tenant";
import { useTeacherClient } from "../../../lib/teacher-client";
import { calculateGradePlanResult, type GradeStudentLike } from "../../../lib/grade-plan";
import { useGradePlan } from "../../../lib/use-grade-plan";
import "./mastery-dashboard.css";

type Student = GradeStudentLike & { id:string; storageId?:string; name?:string; class?:string; className?:string; code?:string; accessCode?:string; studentCode?:string; teacherNote?:string };
type SchoolClass = { id:string; name:string; grade?:number; section?:string };
type StatusFilter = "all" | "mastered" | "support" | "incomplete";
type MasteryRow = Student & { sectionLabel:string; earned:number; maximum:number; percentage:number|null; status:StatusFilter };

function aliases(student:Student){return [...new Set([student.id,student.code,student.accessCode,student.studentCode].map(v=>String(v||"").trim()).filter(Boolean))];}
function fmt(n:number){return Number.isInteger(n)?String(n):n.toFixed(1).replace(/\.0$/,"");}

export default function MasteryDashboard(){
  const session=useTeacherClient();
  const {activePlan}=useGradePlan(true);
  const teacherId=session.teacherId||"",teacherName=session.teacherName||"المعلم",subjectKey=session.subjectKey||"history",subject=session.subject||"المادة",activeGrade=session.activeGrade||null;
  const [storedStudents,setStoredStudents]=useState<Student[]>([]),[scopeStudents,setScopeStudents]=useState<Student[]>([]),[scopeClasses,setScopeClasses]=useState<SchoolClass[]>([]);
  const [loading,setLoading]=useState(false),[selectedClass,setSelectedClass]=useState(""),[selectedSection,setSelectedSection]=useState(""),[threshold,setThreshold]=useState(80),[statusFilter,setStatusFilter]=useState<StatusFilter>("support");
  const [selectedIds,setSelectedIds]=useState<string[]>([]),[message,setMessage]=useState(""),[referralOpen,setReferralOpen]=useState(false),[reason,setReason]=useState("انخفاض مستوى الإتقان والتحصيل الدراسي");
  const referralsPath=useMemo(()=>teacherId?tenantCollection(teacherId,subjectKey as never,"counselorReferrals"):"",[teacherId,subjectKey]);

  useEffect(()=>{if(!teacherId||!subjectKey)return setStoredStudents([]);let active=true;const controller=new AbortController();fetch(`/api/teacher/grade-data?subjectId=${encodeURIComponent(String(subjectKey).split("--")[0])}`,{credentials:"same-origin",signal:controller.signal}).then(async r=>{const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.message||"تعذر تحميل الدرجات.");return data;}).then(data=>{if(!active)return;const byCode=data.byCode&&typeof data.byCode==="object"?data.byCode:{};setStoredStudents(Object.entries(byCode).map(([code,row])=>({...(row as object),id:String((row as any).documentId||code),code:String((row as any).code||(row as any).accessCode||(row as any).studentCode||code)})) as Student[]);}).catch(error=>{if(active&&error?.name!=="AbortError")setMessage(error instanceof Error?error.message:"تعذر تحميل الدرجات.");});return()=>{active=false;controller.abort();};},[teacherId,subjectKey]);
  useEffect(()=>{if(!teacherId||!subjectKey||!activeGrade){setScopeStudents([]);setScopeClasses([]);return;}let active=true;const controller=new AbortController();setLoading(true);const params=new URLSearchParams({subjectId:subjectKey,grade:String(activeGrade)});fetch(`/api/teacher/students?${params}`,{signal:controller.signal}).then(async r=>{const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.message||"تعذر تحميل الطلاب.");return data;}).then(data=>{if(active){setScopeStudents(Array.isArray(data.students)?data.students:[]);setScopeClasses(Array.isArray(data.classes)?data.classes:[]);}}).catch(error=>{if(active&&error?.name!=="AbortError")setMessage(error instanceof Error?error.message:"تعذر تحميل الطلاب.");}).finally(()=>{if(active)setLoading(false);});return()=>{active=false;controller.abort();};},[teacherId,subjectKey,activeGrade]);
  useEffect(()=>{if(!activePlan?.sections?.length)return setSelectedSection("");if(!selectedSection||!activePlan.sections.some(s=>s.id===selectedSection))setSelectedSection(activePlan.sections[0].id);},[activePlan,selectedSection]);

  const students=useMemo(()=>{const live=new Map<string,Student>();storedStudents.forEach(s=>aliases(s).forEach(a=>live.set(a,s)));return scopeStudents.map(roster=>{const found=aliases(roster).map(a=>live.get(a)).find(Boolean);const cls=String(roster.className||roster.class||"").trim();return {...roster,...(found||{}),id:roster.id,storageId:found?.id||roster.id,class:cls,className:cls};}).sort((a,b)=>(a.name||"").localeCompare(b.name||"","ar"));},[scopeStudents,storedStudents]);
  const classes=useMemo(()=>scopeClasses.map(c=>{const name=String(c.name||"").trim(),section=String(c.section||"").trim();return !section||name.includes(section)?name:`${name} (${section})`;}).filter(Boolean),[scopeClasses]);
  const selectedPlanSection=activePlan?.sections.find(s=>s.id===selectedSection);
  const rows=useMemo<MasteryRow[]>(()=>{if(!activePlan||!selectedSection)return[];return students.filter(s=>!selectedClass||(s.class||"")===selectedClass||selectedClass.startsWith(`${s.class||""} (`)).map(student=>{const result=calculateGradePlanResult(activePlan,student);const section=result.sections.find(s=>s.id===selectedSection);if(!section||section.maximum<=0)return {...student,sectionLabel:selectedPlanSection?.label||"—",earned:0,maximum:0,percentage:null,status:"incomplete"};const percentage=Math.max(0,Math.min(100,Math.round((section.earned/section.maximum)*1000)/10));const status:StatusFilter=percentage>=threshold?"mastered":"support";return {...student,sectionLabel:section.label,earned:section.earned,maximum:section.maximum,percentage,status};});},[activePlan,selectedSection,students,selectedClass,threshold,selectedPlanSection?.label]);
  const shownRows=useMemo(()=>statusFilter==="all"?rows:rows.filter(r=>r.status===statusFilter),[rows,statusFilter]);
  const mastered=rows.filter(r=>r.status==="mastered"),support=rows.filter(r=>r.status==="support"),incomplete=rows.filter(r=>r.status==="incomplete"),scored=rows.filter(r=>r.percentage!==null);
  const average=scored.length?Math.round(scored.reduce((a,r)=>a+(r.percentage||0),0)/scored.length*10)/10:0;
  const unitMaximum=rows.find(r=>r.maximum>0)?.maximum||0;
  const selectedSupport=rows.filter(r=>selectedIds.includes(r.id)&&r.status==="support");
  function toggle(id:string,checked:boolean){setSelectedIds(current=>checked?[...new Set([...current,id])]:current.filter(x=>x!==id));}
  function selectAllSupport(){setSelectedIds(support.map(r=>r.id));setStatusFilter("support");}
  async function sendReferral(){if(!selectedSupport.length)return setMessage("حدد طالبًا غير متقن واحدًا على الأقل.");if(!reason.trim())return setMessage("اكتب سبب الإحالة.");const now=new Date().toISOString();await Promise.all(selectedSupport.map(row=>setDoc(doc(db,referralsPath,crypto.randomUUID()),{studentId:row.id,studentName:row.name||"",className:row.class||"",grade:activeGrade,percentage:row.percentage??0,earned:row.earned,maximum:row.maximum,masterySectionId:selectedSection,masterySectionLabel:row.sectionLabel,masteryThreshold:threshold,reason:reason.trim(),referralType:"mastery",referralTypeLabel:"إحالة إتقان وتحصيل",status:"جديدة",teacherId,teacherName,subjectId:subjectKey,subject,explicitTeacherAction:true,createdAt:now})));setMessage(`تمت إحالة ${selectedSupport.length} طالب للمرشد.`);setReferralOpen(false);setSelectedIds([]);}
  function printReport(){const target=shownRows,win=window.open("","_blank","width=1100,height=900");if(!win)return setMessage("اسمح بالنوافذ المنبثقة للطباعة.");const esc=(v:unknown)=>String(v??"—").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]||c));const body=target.map((r,i)=>`<tr><td>${i+1}</td><td class="name">${esc(r.name)}</td><td>${esc(r.class)}</td><td>${r.maximum?`${fmt(r.earned)} / ${fmt(r.maximum)}`:"—"}</td><td>${r.percentage===null?"—":`${fmt(r.percentage)} من 100`}</td><td>${r.status==="mastered"?"متقن":r.status==="support"?"غير متقن":"لا توجد وحدة محسوبة"}</td><td>${esc(r.teacherNote||"—")}</td></tr>`).join("");win.document.write(`<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>تقرير الإتقان</title><style>@page{size:A4 landscape;margin:8mm}*{box-sizing:border-box}body{font-family:Tahoma,Arial;color:#17384a;margin:0}header{border:1px solid #d8e5ed;border-top:5px solid #9a6a34;padding:12px 16px;display:flex;justify-content:space-between}.meta{display:grid;grid-template-columns:repeat(5,1fr);gap:6px;margin:8px 0}.meta div{border:1px solid #dfd6ca;padding:7px;background:#fbf8f3}.meta small{display:block;color:#7c6b5b}table{width:100%;border-collapse:collapse;table-layout:fixed;font-size:9px}th,td{border:1px solid #ddd7cf;padding:6px;text-align:center}th{background:#f2ece4}.name{text-align:right;font-weight:700}</style></head><body><header><div><small>بوابة أستاذ لحوني التعليمية</small><h2>تقرير الإتقان والمتابعة</h2></div><b>${esc(subject)}</b></header><div class="meta"><div><small>المعلم</small><b>${esc(teacherName)}</b></div><div><small>الصف</small><b>${esc(activeGrade)}</b></div><div><small>الفصل</small><b>${esc(selectedClass||"جميع الفصول")}</b></div><div><small>الوحدة</small><b>${esc(selectedPlanSection?.label)}</b></div><div><small>الوحدة كاملة</small><b>${fmt(unitMaximum)} درجة = 100%</b></div></div><table><thead><tr><th>م</th><th>الطالب</th><th>الفصل</th><th>درجة الطالب من الوحدة</th><th>ما حققه من 100</th><th>الحالة</th><th>ملاحظة المعلم</th></tr></thead><tbody>${body||'<tr><td colspan="7">لا توجد بيانات.</td></tr>'}</tbody></table><script>window.onload=()=>setTimeout(()=>window.print(),200)<\/script></body></html>`);win.document.close();}

  if(!teacherId)return <main className="mastery-dashboard" dir="rtl"><p>جارٍ تجهيز الإتقان…</p></main>;
  return <main className="mastery-dashboard" dir="rtl">
    <section className="mastery-head"><div className="mastery-filters">
      <label>الوحدة<select value={selectedSection} onChange={e=>{setSelectedSection(e.target.value);setSelectedIds([]);}}>{activePlan?.sections.map(s=><option key={s.id} value={s.id}>{s.label}</option>)}</select></label>
      <label>الفصل<select value={selectedClass} onChange={e=>{setSelectedClass(e.target.value);setSelectedIds([]);}}><option value="">جميع الفصول</option>{classes.map(c=><option key={c}>{c}</option>)}</select></label>
      <label>معيار الإتقان<select value={threshold} onChange={e=>{setThreshold(Number(e.target.value));setStatusFilter("support");setSelectedIds([]);}}><option value={90}>90%</option><option value={85}>85%</option><option value={80}>80%</option><option value={75}>75%</option><option value={70}>70%</option></select></label>
      <label>عرض الطلاب<select value={statusFilter} onChange={e=>setStatusFilter(e.target.value as StatusFilter)}><option value="support">غير المتقنين</option><option value="mastered">المتقنون</option><option value="all">جميع الطلاب</option><option value="incomplete">لا توجد وحدة محسوبة</option></select></label>
    </div></section>
    {!activePlan&&<div className="mastery-alert">لم تُعتمد خطة توزيع الدرجات بعد.</div>}{loading&&<div className="mastery-alert">جارٍ تحميل بيانات الطلاب…</div>}
    <section className="unit-summary"><div><span>الوحدة المختارة</span><strong>{selectedPlanSection?.label||"—"}</strong></div><div><span>مجموع درجات الوحدة</span><strong>{fmt(unitMaximum)} درجة</strong><small>تمثل 100% من الوحدة</small></div><div><span>متوسط ما حققه الطلاب</span><strong>{fmt(average)} من 100</strong></div></section>
    <section className="mastery-stats"><button onClick={()=>setStatusFilter("all")}><span>إجمالي الطلاب</span><strong>{rows.length}</strong></button><button onClick={()=>setStatusFilter("mastered")}><span>متقنون</span><strong>{mastered.length}</strong><small>{threshold}% فأعلى</small></button><button onClick={()=>setStatusFilter("support")}><span>غير متقنين</span><strong>{support.length}</strong><small>أقل من {threshold}%</small></button><div><span>معيار الإتقان</span><strong>{threshold}%</strong><small>من نسبة الوحدة الكاملة</small></div></section>
    <section className="mastery-panel"><header><div><h2>بيانات تحصيل الوحدة</h2><p>درجة الطالب من مجموع درجات الوحدة، ثم ما حققه من نسبة الوحدة الكاملة (100%).</p></div><div className="mastery-actions"><button onClick={selectAllSupport}>تحديد غير المتقنين</button><button className="referral" onClick={()=>{if(!selectedSupport.length)selectAllSupport();setReferralOpen(true);}}>إحالة المحددين للمرشد</button><button className="print" onClick={printReport}>طباعة القائمة</button></div></header>
      <div className="mastery-table-wrap"><table><thead><tr><th>تحديد</th><th>اسم الطالب</th><th>الصف</th><th>الفصل</th><th>درجة الطالب من الوحدة</th><th>نسبة الوحدة الكاملة</th><th>ما حققه الطالب من 100</th><th>الحالة</th><th>ملاحظة المعلم</th></tr></thead><tbody>{shownRows.map(row=><tr key={row.id}><td><input type="checkbox" disabled={row.status!=="support"} checked={selectedIds.includes(row.id)} onChange={e=>toggle(row.id,e.target.checked)}/></td><td className="student"><b>{row.name||"—"}</b><small>{row.code||row.accessCode||""}</small></td><td>{activeGrade||"—"}</td><td>{row.class||"—"}</td><td className="score"><b>{row.maximum?`${fmt(row.earned)} / ${fmt(row.maximum)}`:"—"}</b></td><td><span className="unit-full">100%</span></td><td><strong className={`achieved ${row.status}`}>{row.percentage===null?"—":`${fmt(row.percentage)} من 100`}</strong></td><td><span className={`status ${row.status}`}>{row.status==="mastered"?"متقن":row.status==="support"?"غير متقن":"لا توجد وحدة محسوبة"}</span></td><td className="note">{row.teacherNote||"—"}</td></tr>)}</tbody></table>{!shownRows.length&&<p className="empty">لا توجد بيانات مطابقة للاختيار الحالي.</p>}</div>
    </section>
    {referralOpen&&<div className="mastery-modal" onClick={()=>setReferralOpen(false)}><section onClick={e=>e.stopPropagation()}><header><div><h3>إحالة غير المتقنين للمرشد</h3><p>{selectedSupport.length} طالب • {selectedPlanSection?.label}</p></div><button onClick={()=>setReferralOpen(false)}>×</button></header><div className="referral-list">{selectedSupport.map(r=><div key={r.id}><b>{r.name}</b><span>{r.class} • {fmt(r.earned)}/{fmt(r.maximum)} • {fmt(r.percentage||0)} من 100</span></div>)}</div><label>سبب الإحالة<textarea value={reason} onChange={e=>setReason(e.target.value)}/></label><footer><button onClick={()=>setReferralOpen(false)}>إلغاء</button><button className="confirm" onClick={()=>void sendReferral()}>تسجيل الإحالة</button></footer></section></div>}
    {message&&<div className="mastery-toast">{message}</div>}
  </main>;
}
