"use client";

import { useEffect, useMemo, useState } from "react";
import { collection, doc, onSnapshot, updateDoc } from "firebase/firestore";
import { db } from "../../../lib/firebase";
import "./follow-up.css";

type UnitRecord={attendance?:number;participation?:number;homework?:number;unitExam?:number;total?:number;maximumTotal?:number;percentage?:number};
type Student={id:string;name?:string;class?:string;nationalId?:string;researchScore?:number;teacherNote?:string;units?:Record<string,UnitRecord>;parentCounselorNoticeSentAt?:string;parentCounselorNoticeSeenAt?:string};
const unitKeys=["unit1","unit2","unit3","unit4","unit5"];
const unitLabels:Record<string,string>={unit1:"الوحدة الأولى",unit2:"الوحدة الثانية",unit3:"الوحدة الثالثة",unit4:"الوحدة الرابعة",unit5:"الوحدة الخامسة"};
const counselors=["966568339922","966598353651","966555884410"];
function studentTotal(student:Student){const unitsTotal=unitKeys.reduce((sum,key)=>{const r=student.units?.[key]||{};return sum+Number(r.total??((r.attendance||0)+(r.participation||0)+(r.homework||0)+(r.unitExam||0)));},0);return unitsTotal+Number(student.researchScore||0);}
function missingCount(student:Student){let count=0;unitKeys.forEach(key=>{const r=student.units?.[key]||{};if(r.attendance===undefined)count++;if(r.participation===undefined)count++;if(r.homework===undefined)count++;if(r.unitExam===undefined)count++;});if(student.researchScore===undefined)count++;return count;}
function level(total:number){if(total>=90)return{label:"متقن بتميز",className:"excellent"};if(total>=80)return{label:"متقن",className:"mastered"};if(total>=60)return{label:"غير متقن",className:"warning"};return{label:"يحتاج تدخلاً",className:"danger"};}
function unitSummary(student:Student){const items=unitKeys.map(key=>{const record=student.units?.[key];if(!record)return null;const total=Number(record.total??((record.attendance||0)+(record.participation||0)+(record.homework||0)+(record.unitExam||0)));const maximum=Number(record.maximumTotal||19);const percentage=Number(record.percentage??Math.round(total/maximum*100));return percentage<80?`${unitLabels[key]} (${percentage}٪)`:null;}).filter(Boolean) as string[];return items.length?items.join("، "):"غير محدد";}
export default function FollowUpPage(){
 const [students,setStudents]=useState<Student[]>([]);const [selectedClass,setSelectedClass]=useState("");const [threshold,setThreshold]=useState(80);const [noteStudent,setNoteStudent]=useState<Student|null>(null);const [note,setNote]=useState("");const [saved,setSaved]=useState("");const [reportOpen,setReportOpen]=useState(false);const [notifyParents,setNotifyParents]=useState(false);const [selectedCounselor,setSelectedCounselor]=useState(counselors[0]);
 useEffect(()=>onSnapshot(collection(db,"students"),snap=>{const list=snap.docs.map(d=>({id:d.id,...d.data()})) as Student[];list.sort((a,b)=>(a.name||"").localeCompare(b.name||"","ar"));setStudents(list);}),[]);
 const classes=useMemo(()=>Array.from(new Set(students.map(s=>(s.class||"").trim()).filter(Boolean))).sort((a,b)=>a.localeCompare(b,"ar")),[students]);const visible=useMemo(()=>students.filter(s=>!selectedClass||(s.class||"").trim()===selectedClass),[students,selectedClass]);const ranked=useMemo(()=>visible.map(s=>({...s,total:studentTotal(s),missing:missingCount(s)})).sort((a,b)=>b.total-a.total),[visible]);const nonMastered=useMemo(()=>ranked.filter(s=>s.total<threshold),[ranked,threshold]);const average=ranked.length?Math.round(ranked.reduce((sum,s)=>sum+s.total,0)/ranked.length):0;const incomplete=ranked.filter(s=>s.missing>0);
 function openReport(){if(!nonMastered.length){setSaved("لا يوجد طلاب يحتاجون متابعة حالياً.");setTimeout(()=>setSaved(""),2500);return;}setNotifyParents(false);setReportOpen(true);}
 function printList(){document.body.classList.add("printing-follow-up");window.print();setTimeout(()=>document.body.classList.remove("printing-follow-up"),300);}function exportPdf(){printList();}
 async function sendToCounselor(){if(notifyParents){const now=new Date().toISOString();await Promise.all(nonMastered.map(s=>{if(s.parentCounselorNoticeSentAt)return Promise.resolve();return updateDoc(doc(db,"students",s.id),{parentCounselorNoticeSentAt:now});}));}const whatsappMessage=`السلام عليكم ورحمة الله وبركاته،\n\nتم رفع أسماء الطلاب الذين لم يحققوا نسبة الإتقان المطلوبة في مادة التاريخ:\n\n${nonMastered.map((s,i)=>`${i+1}. ${s.name||"—"} — ${studentTotal(s)}%`).join("\n")}\n\nالمعلم: الأستاذ حسن علي الطويل\nمادة التاريخ – مدرسة التهذيب الثانوية`;window.open(`https://wa.me/${selectedCounselor}?text=${encodeURIComponent(whatsappMessage)}`,"_blank");setSaved(notifyParents?"تم تجهيز البلاغ وفتح واتساب، وسيظهر تنبيه ولي الأمر عند دخوله البوابة لأول مرة.":"تم فتح واتساب بالبلاغ الجاهز");setReportOpen(false);setTimeout(()=>setSaved(""),3500);}
 async function copyList(){const lines=["الطلاب غير المتقنين في مادة التاريخ",`الفصل: ${selectedClass||"جميع الفصول"}`,`المعيار: أقل من ${threshold}%`,"",...nonMastered.map((s,i)=>`${i+1}. ${s.name||"—"} | ${s.class||"—"} | ${s.nationalId||"—"} | ${s.total}/100`)];await navigator.clipboard.writeText(lines.join("\n"));setSaved("تم نسخ القائمة للموجّه الطلابي");setTimeout(()=>setSaved(""),2200);}
 async function saveNote(){if(!noteStudent)return;await updateDoc(doc(db,"students",noteStudent.id),{teacherNote:note.trim()});setSaved("تم حفظ ملاحظة المعلم وتظهر الآن لولي الأمر");setTimeout(()=>setSaved(""),2500);}
 return <main className="follow-page" dir="rtl"><section className="follow-head"><div><span>متابعة التحصيل الدراسي</span><h1>الإتقان والتنبيهات الذكية</h1><p>قائمة فورية للطلاب الأقل من معيار الإتقان مع أدوات جاهزة للموجّه الطلابي.</p></div><div className="follow-filters"><label>الفصل<select value={selectedClass} onChange={e=>setSelectedClass(e.target.value)}><option value="">جميع الفصول</option>{classes.map(c=><option key={c}>{c}</option>)}</select></label><label>معيار الإتقان<select value={threshold} onChange={e=>setThreshold(Number(e.target.value))}><option value={80}>أقل من ٨٠٪</option><option value={75}>أقل من ٧٥٪</option><option value={70}>أقل من ٧٠٪</option></select></label></div></section><section className="follow-overview"><article><span>عدد الطلاب</span><strong>{ranked.length}</strong></article><article><span>متوسط الفصل</span><strong>{average}%</strong></article><article className="warn"><span>غير المتقنين</span><strong>{nonMastered.length}</strong></article><article className="alert"><span>ناقصو الرصد</span><strong>{incomplete.length}</strong></article></section><section className="follow-grid"><article className="follow-card top-card"><header><div><h2>أفضل الطلاب</h2><p>الترتيب حسب المجموع النهائي.</p></div></header><div className="top-students">{ranked.slice(0,3).map((s,i)=><div key={s.id}><b>{["🥇","🥈","🥉"][i]}</b><span>{s.name}</span><strong>{s.total}</strong><small>{s.total-average>=0?`أعلى من المتوسط بـ ${s.total-average}`:`أقل من المتوسط بـ ${average-s.total}`}</small></div>)}</div></article><article className="follow-card alerts-card"><header><div><h2>تنبيهات الرصد</h2><p>طلاب لديهم خانات لم تُرصد بعد.</p></div></header><div className="alerts-list">{incomplete.slice(0,8).map(s=><div key={s.id}><span>{s.name}</span><b>{s.missing} خانة ناقصة</b></div>)}{!incomplete.length&&<p className="empty">جميع الدرجات مكتملة.</p>}</div></article></section><section className="follow-card nonmastered-card print-follow-target"><header><div><h2>الطلاب غير المتقنين</h2><p>أقل من {threshold}% — جاهزة للنسخ أو الطباعة والإبلاغ للموجّه الطلابي.</p></div><div className="follow-actions"><button onClick={copyList}>نسخ القائمة</button><button className="counselor-button" onClick={openReport}>📱 إبلاغ الموجه عبر واتساب</button><button onClick={printList}>طباعة القائمة</button></div></header><div className="follow-print-title"><h2>مدرسة التهذيب الثانوية</h2><p>قائمة الطلاب غير المتقنين في مادة التاريخ</p><small>إعداد / الأستاذ حسن علي الطويل</small></div><div className="follow-table-wrap"><table><thead><tr><th>م</th><th>اسم الطالب</th><th>الفصل</th><th>السجل المدني</th><th>المجموع</th><th>المستوى</th><th>مقارنة بالمتوسط</th><th>ملاحظة</th></tr></thead><tbody>{nonMastered.map((s,i)=>{const l=level(s.total);return <tr key={s.id}><td>{i+1}</td><td><b>{s.name}</b></td><td>{s.class}</td><td>{s.nationalId||"—"}</td><td><strong>{s.total}/100</strong></td><td><span className={`level ${l.className}`}>{l.label}</span></td><td>{s.total-average>=0?`+${s.total-average}`:s.total-average}</td><td><button className="note-btn" onClick={()=>{setNoteStudent(s);setNote(s.teacherNote||"")}}>إضافة ملاحظة</button></td></tr>})}</tbody></table>{!nonMastered.length&&<p className="empty">لا يوجد طلاب أقل من معيار الإتقان المحدد.</p>}</div></section>{reportOpen&&<div className="report-modal" onClick={()=>setReportOpen(false)}><section onClick={e=>e.stopPropagation()}><div className="report-modal-head"><div><span>تقرير جاهز للإرسال</span><h3>إبلاغ الموجه عبر واتساب</h3><p>{nonMastered.length} طالبًا غير متقن — المعيار أقل من {threshold}%</p></div><button className="report-close" onClick={()=>setReportOpen(false)} aria-label="إغلاق">×</button></div><div className="report-table-wrap"><table><thead><tr><th>م</th><th>اسم الطالب</th><th>النسبة</th></tr></thead><tbody>{nonMastered.map((s,i)=><tr key={s.id}><td>{i+1}</td><td><b>{s.name||"—"}</b></td><td>{s.total}%</td></tr>)}</tbody></table></div><label className="parent-notify-option"><input type="checkbox" checked={notifyParents} onChange={e=>setNotifyParents(e.target.checked)}/><span><b>إبلاغ ولي الأمر من خلال البوابة</b><small>سيظهر تنبيه لولي الأمر عند أول إبلاغ فقط لكل طالب.</small></span></label><div style={{display:"grid",gap:10,marginBottom:12}}><label style={{fontWeight:700}}>اختر الموجه</label><select value={selectedCounselor} onChange={e=>setSelectedCounselor(e.target.value)} style={{padding:"10px 12px",borderRadius:10}}><option value="966568339922">الموجه الأول</option><option value="966598353651">الموجه الثاني</option><option value="966555884410">الموجه الثالث</option></select></div><div className="report-modal-actions"><button onClick={()=>setReportOpen(false)}>إلغاء</button><button onClick={exportPdf}>📄 تصدير PDF</button><button onClick={printList}>🖨️ طباعة</button><button className="primary" onClick={sendToCounselor}>📱 فتح واتساب بالبلاغ</button></div></section></div>}{noteStudent&&<div className="note-modal" onClick={()=>setNoteStudent(null)}><section onClick={e=>e.stopPropagation()}><h3>ملاحظة المعلم</h3><p>{noteStudent.name}</p><textarea value={note} onChange={e=>setNote(e.target.value)} placeholder="مثال: يحتاج إلى متابعة في اختبارات الوحدات وأداء الواجبات..."/><div><button onClick={()=>setNoteStudent(null)}>إلغاء</button><button onClick={saveNote}>حفظ وإظهارها لولي الأمر</button></div></section></div>}{saved&&<div className="follow-toast">{saved}</div>}</main>;
}
