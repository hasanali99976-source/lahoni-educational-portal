"use client";

/**
 * Competition is intentionally retired. This compatibility component renders
 * nothing and performs no fetches, listeners, timers, or Firestore reads.
 */
export default function TeacherCompetitionProgress(_: { compact?: boolean }) {
  return null;
}
