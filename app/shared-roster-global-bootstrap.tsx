"use client";

/**
 * المزامنة التلقائية الشاملة متوقفة عمدًا.
 * كانت تكتب ملفات الطلاب في كل لقطة Firestore، فتُعيد تشغيل نفسها وتستهلك الحصة اليومية.
 * تبقى إضافة الطلاب وتعديلهم من صفحة إدارة الطلاب فقط حتى تُستبدل المزامنة بعملية محدودة وآمنة.
 */
export default function SharedRosterGlobalBootstrap() {
  return null;
}
