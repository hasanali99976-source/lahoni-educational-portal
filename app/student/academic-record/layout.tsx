import SectionDeductionRuntime from "./section-deduction-runtime";

export default function AcademicRecordLayout({ children }: { children: React.ReactNode }) {
  return <><SectionDeductionRuntime />{children}</>;
}
